########## ENV ##########
export XDG_CACHE_HOME=~/.cache
export XDG_CONFIG_HOME=~/.config
export XDG_DATA_HOME=~/.local/share
export XDG_BIN_HOME=~/.local/bin
export BASHRC_FILE_LOC="$HOME/.bashrc"
export ZSHRC_FILE_LOC="$XDG_CONFIG_HOME/zsh/.zshrc"

########## Program Variables ##########
export VIM_HOME_DIR="$XDG_CONFIG_HOME/nvim"
export FZF_SOURCE_DIR="$XDG_CONFIG_HOME/.fzf"
export TMUX_HOME="$XDG_CONFIG_HOME/tmux"

########## Editors ##########
export EDITOR='nvim'
export GIT_EDITOR="$EDITOR"
export USE_EDITOR="$EDITOR"
export VISUAL=$EDITOR

########## Paths ##########
[ -d "$HOME/bin" ] && export PATH="$HOME/bin:$PATH"

########## Pager and flow: ##########
export PAGER=less
export LESS='-iFMRSX -x4'
# make less more friendly for non-text input files, see lesspipe(1)
test -x /usr/bin/lesspipe && eval "$(SHELL=/bin/sh lesspipe)"
# disable flow control (ctrl + s,q)
stty -ixon

# Ignore basic commands in history
export HISTORY_IGNORE="(ls|bg|fg|pwd|exit|cd ..)"

umask 022
