#
# This file is sourced by `zsh` interactive shells.
# It is read after ~/.zprofile and /etc/zsh/zshrc.
#

[ -r "${HOME}/.config/shell/zsh/interactive.zsh" ] && . "${HOME}/.config/shell/zsh/interactive.zsh"
