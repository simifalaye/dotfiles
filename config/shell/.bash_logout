# shellcheck disable=SC2148
# vim: filetype=bash
#
# This file is sourced by `bash` login shells on logout.
# It is read before /etc/bash.bash_logout.
#

[ -r "${HOME}/.config/shell/bash/logout.bash" ] && . "${HOME}/.config/shell/bash/logout.bash"
